
/*=======================================
  CSS RESPONSIVO - Z-CORE PREMIUM
  =======================================*/

/* TELAS GRANDES (1025px - 1400px) */
@media (max-width: 1400px) {
  .hero-container, .header-container, .footer-content, .testimonials-premium, .trust-container {
    padding-left: 40px;
    padding-right: 40px;
  }
}

/* TELAS MÉDIAS (769px - 1024px) */
@media (max-width: 1024px) {
  .hero-container {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 60px;
  }
  .hero-content { align-items: center; }
  .hero-stats { justify-content: center; }
  .hero-title { font-size: 56px; }

  .testimonials-grid { grid-template-columns: repeat(2, 1fr); }
  
  .banner-premium { padding: 60px; }
  .banner-container { flex-direction: column; text-align: center; gap: 40px; }
  .banner-text { max-width: 100%; }

  .newsletter-container-premium { 
    grid-template-columns: 1fr; 
    text-align: center; 
    padding: 60px;
    gap: 40px;
  }
}

/* TELAS PEQUENAS (até 768px) */
@media (max-width: 768px) {
  .hero-title { font-size: 42px; }
  .hero-actions { flex-direction: column; width: 100%; }
  .btn-primary-premium, .btn-secondary-premium { width: 100%; text-align: center; }

  .trust-container { grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 30px; }
  .testimonials-grid { grid-template-columns: 1fr; }
  
  .banner-premium { margin: 60px 20px; padding: 40px; }
  .banner-title { font-size: 32px; }

  .footer-content { grid-template-columns: 1fr; gap: 40px; }
  .footer-bottom { flex-direction: column; text-align: center; gap: 20px; }

  /* Produtos: 2 colunas em tablets */
  .prod-grid { grid-template-columns: repeat(2, 1fr); gap: 20px; }
  .pcard { margin: 0; }

  /* Categorias: 3 colunas em tablets */
  .cat-grid { grid-template-columns: repeat(3, 1fr); gap: 15px; }
}

/* MOBILE (até 480px) */
@media (max-width: 480px) {
  .hero-title { font-size: 36px; }
  .hero-stats { flex-direction: column; gap: 30px; align-items: center; }
  
  .trust-container { grid-template-columns: 1fr; }
  
  .newsletter-container-premium { padding: 40px 20px; border-radius: 30px; }
  .newsletter-title { font-size: 28px; }
  
  .banner-premium { border-radius: 30px; margin: 40px 15px; padding: 30px; }
  .btn-banner-premium { width: 100%; justify-content: center; }

  .section-header-premium h2 { font-size: 28px; }

  /* Produtos: 1 coluna em celulares */
  .prod-grid { grid-template-columns: 1fr; gap: 20px; padding: 0 15px; }
  .pcard { max-width: 100%; }

  /* Categorias: 2 colunas em celulares */
  .cat-grid { grid-template-columns: repeat(2, 1fr); gap: 12px; padding: 0 15px; }

  /* Header com espaçamento menor */
  .header-container { padding: 0 15px; }
  .logo-text { font-size: 20px; }

  /* Busca com tamanho adequado */
  .search-container { max-width: 140px; }

  /* Hero com padding menor */
  .hero-container { padding: 60px 15px; }
  .hero-subtitle { font-size: 16px; }

  /* Ajustes nas seções */
  .section-header-premium { padding: 0 15px; }
  .testimonials-premium { padding: 60px 15px; }
  .trust-container { padding: 40px 15px; }
}
