services:
  api:
    build: 
      context: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=prod-secret-change-me
      - ADMIN_USER=admin
      - ADMIN_PASS=admin123
    volumes:
      - ./backend/data:/app/backend/data
